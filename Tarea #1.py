# Crea un nuevo proyecto en PyCharm y un archivo Python para este programa.

# Escribe un programa que incluya una matriz bidimensional (puede ser una matriz pequeña de 3x3) con valores numéricos.

# Implementa una función que realice una búsqueda en la matriz para encontrar un valor específico que definas.

# Muestra un mensaje que indique si el valor se encontró o no, y muestra su posición en la matriz si se encontró.

matriz = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

numero_buscar = int(input("Introduce un valor a buscar en la matriz: "))

for filas in matriz:
    for valor in filas:
        if valor == numero_buscar:
            print("El valor se encontro en la matriz")
            print(valor)
            break
else:
    print("El valor no se encontro en la matriz")
# Crea un nuevo proyecto en PyCharm y un archivo Python para este programa.

# Escribe un programa que incluya una matriz bidimensional (puede ser una matriz pequeña de 3x3) con valores numéricos.

# Implementa una función que realice una búsqueda en la matriz para encontrar un valor específico que definas.

# Muestra un mensaje que indique si el valor se encontró o no, y muestra su posición en la matriz si se encontró.

matriz = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

numero_buscar = int(input("Introduce un valor a buscar en la matriz: "))

for filas in matriz:
    for valor in filas:
        if valor == numero_buscar:
            print("El valor se encontro en la matriz")
            print(valor)
            break
else:
    print("El valor no se encontro en la matriz")
# Crea un nuevo proyecto en PyCharm y un archivo Python para este programa.

# Escribe un programa que incluya una matriz bidimensional (puede ser una matriz pequeña de 3x3) con valores numéricos.

# Implementa una función que realice una búsqueda en la matriz para encontrar un valor específico que definas.

# Muestra un mensaje que indique si el valor se encontró o no, y muestra su posición en la matriz si se encontró.

matriz = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

numero_buscar = int(input("Introduce un valor a buscar en la matriz: "))

for filas in matriz:
    for valor in filas:
        if valor == numero_buscar:
            print("El valor se encontro en la matriz")
            print(valor)
            break
else:
    print("El valor no se encontro en la matriz")
# Crea un nuevo proyecto en PyCharm y un archivo Python para este programa.

# Escribe un programa que incluya una matriz bidimensional (puede ser una matriz pequeña de 3x3) con valores numéricos.

# Implementa una función que realice una búsqueda en la matriz para encontrar un valor específico que definas.

# Muestra un mensaje que indique si el valor se encontró o no, y muestra su posición en la matriz si se encontró.

matriz = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

numero_buscar = int(input("Introduce un valor a buscar en la matriz: "))

for filas in matriz:
    for valor in filas:
        if valor == numero_buscar:
            print("El valor se encontro en la matriz")
            print(valor)
            break
else:
    print("El valor no se encontro en la matriz")
